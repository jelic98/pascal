<<<<<<< HEAD
{$APPTYPE CONSOLE}

var
  n,q,x,y,l,i,j,res:longint;
  s1,s2,s3,s4:ansistring;

begin
  readln(n);

  readln(s1);
  readln(s2);

  readln(q);

  for i:=1 to q do
    begin
      readln(x,y,l);

      res:=0;

      s3:=copy(s1,x,l);
      s4:=copy(s2,y,l);

      for j:=1 to l do
        begin
          if copy(s3,j,1)=copy(s4,j,1) then res:=res+1;
        end;
        
      writeln(res);
    end;
  readln;
end.
=======
{$APPTYPE CONSOLE}

var
  n,q,x,y,l,i,j,res:longint;
  s1,s2,s3,s4:ansistring;

begin
  readln(n);

  readln(s1);
  readln(s2);

  readln(q);

  for i:=1 to q do
    begin
      readln(x,y,l);

      res:=0;

      s3:=copy(s1,x,l);
      s4:=copy(s2,y,l);

      for j:=1 to l do
        begin
          if copy(s3,j,1)=copy(s4,j,1) then res:=res+1;
        end;
        
      writeln(res);
    end;
  readln;
end.
>>>>>>> 1c88b46f80c2c3868de9ed70727efa324560b2fa
