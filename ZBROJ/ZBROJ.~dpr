<<<<<<< HEAD
{$APPTYPE CONSOLE}

var
  a_min,b_min,a_max,b_max,kod,min,max:integer;
  a,b,min_a,min_b,max_a,max_b,a2,b2:string;

begin
  readln(a,b);

  a2:=a;
  b2:=b;

  //if (pos('6',a)=0) and (pos('5',a)=0) then
    //begin

    //end;

  //if (pos('6',a)>0) or (pos('5',a)>0) then
    //begin
          repeat
            insert('5',a,pos('6',a));
            delete(a,pos('6',a),1);
            //min_a:=a;
          until
            pos('6',a)=0;

          repeat
            insert('6',a2,pos('5',a2));
            delete(a2,pos('5',a2),1);
            //max_a:=a2;
          until
          pos('5',a2)=0;
    //end;

  //if (pos('6',b)=0) and (pos('5',b)=0) then
    //begin

    //end;

  //if (pos('6',b)>0) or (pos('5',b)>0) then
    //begin
          repeat
            insert('5',b,pos('6',b));
            delete(b,pos('6',b),1);
            //min_b:=b;
          until
            pos('6',b)=0;

          repeat
            insert('6',b2,pos('5',b2));
            delete(b2,pos('5',b2),1);
            //max_b:=b2;
          until
          pos('5',b2)=0;
    //end;

      min_a:=a;
      max_a:=a2;
      min_b:=b;
      max_b:=b2;

  writeln(min_a);
  writeln(max_a);
  writeln(min_b);
  writeln(max_b);

  val(min_a,a_min,kod);
  val(max_a,a_max,kod);
  val(min_b,b_min,kod);
  val(max_b,b_max,kod);

  min:=a_min+b_min;
  max:=a_max+b_max;

  //writeln(min,' ',max);

  readln;
end.
=======
{$APPTYPE CONSOLE}

var
  a_min,b_min,a_max,b_max,kod,min,max:integer;
  a,b,min_a,min_b,max_a,max_b,a2,b2:string;

begin
  readln(a,b);

  a2:=a;
  b2:=b;

  //if (pos('6',a)=0) and (pos('5',a)=0) then
    //begin

    //end;

  //if (pos('6',a)>0) or (pos('5',a)>0) then
    //begin
          repeat
            insert('5',a,pos('6',a));
            delete(a,pos('6',a),1);
            //min_a:=a;
          until
            pos('6',a)=0;

          repeat
            insert('6',a2,pos('5',a2));
            delete(a2,pos('5',a2),1);
            //max_a:=a2;
          until
          pos('5',a2)=0;
    //end;

  //if (pos('6',b)=0) and (pos('5',b)=0) then
    //begin

    //end;

  //if (pos('6',b)>0) or (pos('5',b)>0) then
    //begin
          repeat
            insert('5',b,pos('6',b));
            delete(b,pos('6',b),1);
            //min_b:=b;
          until
            pos('6',b)=0;

          repeat
            insert('6',b2,pos('5',b2));
            delete(b2,pos('5',b2),1);
            //max_b:=b2;
          until
          pos('5',b2)=0;
    //end;

      min_a:=a;
      max_a:=a2;
      min_b:=b;
      max_b:=b2;

  writeln(min_a);
  writeln(max_a);
  writeln(min_b);
  writeln(max_b);

  val(min_a,a_min,kod);
  val(max_a,a_max,kod);
  val(min_b,b_min,kod);
  val(max_b,b_max,kod);

  min:=a_min+b_min;
  max:=a_max+b_max;

  //writeln(min,' ',max);

  readln;
end.
>>>>>>> 1c88b46f80c2c3868de9ed70727efa324560b2fa
