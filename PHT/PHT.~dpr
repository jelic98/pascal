{$APPTYPE CONSOLE}

var
  t,i,n:longint;
  res:extended;

begin
  readln(t);

  for i:=1 to t do
    begin
      readln(n);

      res:=sqrt(n+1)-1;

      writeln('Case ',i,': ',trunc(res));
    end;

  readln;
end.
 