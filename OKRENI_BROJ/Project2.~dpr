{$APPTYPE CONSOLE}

var
  n,j,m:int64;

begin
  readln(n);

  m:=0;

  while(n<>0) do
    begin
      j:=n mod 10;
      m:=m*10;
      m:=m+j;
      n:=n div 10;
    end;

  writeln(m);
  readln;
end.
