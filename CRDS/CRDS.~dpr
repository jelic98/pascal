{$APPTYPE CONSOLE}

var
  x,y,n,i,j:longint;

begin
  readln(n);

  for i:=1 to n do
    begin
      readln(x);
      y:=0;
      for j:=1 to x do y:=y+3*j-1;
      writeln(y);
    end;

  readln;
end.
